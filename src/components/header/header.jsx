import "./header.css";

const Header = () => {
  return (
    <div className="header-container">
      <h3>Task Tracker</h3>
      <button className="btn btn-primary">Add Task</button>
    </div>
  );
};

export default Header;
